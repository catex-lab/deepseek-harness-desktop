node_modules/
target/
dist/
.tar-cache/
.tar-cache-missing/
*.tgz
pnpm-debug.log

# --- Distribution / build artifacts (large binaries, not source) ---
# These are generated/redistributable outputs. They must NOT be committed:
# pushing them wastes repo space and can leak local builds. Normal use and
# upgrades are unaffected because the files still exist on disk and are
# regenerated by the build/fetch scripts.
*.zip
portable/
DeepSeek-Harness-portable/
src-tauri/resources/engine/node.exe

# Local test / build logs
engine_test.*
*.err
*.out
